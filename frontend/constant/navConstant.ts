import { INavItems } from "@/types/navTypes.type";

export const navItems: INavItems[] = [
    {
        name: "Home",
        path: "/"
    },
    {
        name: "Services",
        path: "/"
    }
]